- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.p10k: p10k
    ~/.config/vifm: vifm/

- shell:
  - [git submodule update --init --recursive, Installing submodules]
