- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/vifm: vifm/
    ~/.fehbg: fehbg
    ~/.gitconfig: gitconfig
    ~/.p10k.zsh: p10k.zsh
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
