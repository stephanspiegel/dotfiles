- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/kitty: kitty/
    ~/.config/nvim: nvim/
    ~/.config/vifm: vifm/
    ~/.config/xmobar: xmobar/
    ~/.xmonad: xmonad/
    ~/.fehbg: fehbg
    ~/.gitconfig: gitconfig
    ~/.p10k.zsh: zsh/p10k.zsh
    ~/.vim: vim/
    ~/.zshenv: zsh/zshenv
    ~/.zshrc: zsh/zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
